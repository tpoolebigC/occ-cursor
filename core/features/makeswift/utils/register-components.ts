import { runtime } from './runtime';

/**
 * Register Makeswift Components
 * Imports all Makeswift components to register them with the runtime
 */

// Import all Makeswift components to register them with the runtime
import '../components'; 