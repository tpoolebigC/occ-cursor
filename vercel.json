{
  "buildCommand": "pnpm install && pnpm run build --filter=@bigcommerce/catalyst-core",
  "outputDirectory": "core/.next",
  "installCommand": "echo 'Dependencies installed via buildCommand'",
  "framework": "nextjs"
} 